# PostgreSQL — đổi user, password, host, port, tên DB cho đúng môi trường
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/DATN?schema=public"

PORT=4000
FRONTEND_ORIGIN=http://localhost:3000
